# Vercel: Project → Settings → Environment Variables (Production + Preview)
# Your Render web service URL — no trailing slash, no /api suffix
# Example: https://orakitech-api.onrender.com
VITE_API_URL=
